import { Express, Request, Response } from "express";
import { IStorage } from "./storage";

export function setupRoutes(app: Express, storage: IStorage) {
  // Routes are now handled directly in index.ts
}